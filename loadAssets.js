
const assetBoxes = document.querySelectorAll('.box');

assetBoxes.forEach((box)=>{
    const type = box.getAttribute('data-load');

    //load in the element box a div with all the images in the type folder
})